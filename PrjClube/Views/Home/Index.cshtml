@model IEnumerable<Clube.Modelo.Modelo.Doacao>


@{
    ViewBag.Title = "Clube do Bem";
}

<div class="content-wrapper">
    <section class="content-header">
        <div class="col-lg-4 pull-left"><h3>Planilha Financeira <small>Visão Geral</small></h3></div>
        <div class="col-lg-2 pull-right">
            @*<a class="btn btn-primary edit" role="button" href="@Url.Action("Cadastrar","Participante")">Novo </a>*@
            <button class="btn btn-info edit" onclick="AbrirModal('#modal','_mdlNovaDoacao')">Registrar Doação</button>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="col-lg-2">
                <select class="form-control" name="tipoFiltro" id="tipoFiltro">
                    <option value="0">Por Id</option>
                </select>
            </div>

            <div class="col-lg-4">
                <div class="input-group input-group-sm">
                    <input type="text" class="form-control" id="filtro" name="filtro" placeholder="Escolha seu tipo de filtro e pesquise aqui..." autofocus>
                    <span class="input-group-btn">
                        <button class="btn btn-info btn-flat" id="filtro-btn">
                            Filtrar...
                        </button>
                    </span>
                </div><!-- /input-group -->
            </div>
        }
    </section>
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">

                    <div class="row">

                        <div class="col-sm-12">
                            <table id="datatable" class="table table-striped table-bordered dataTable no-footer" role="grid" aria-describedby="datatable_info">
                                <thead>
                                    <tr role="row">
                                        <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Name: activate to sort column ascending" style="width: 99px;">Nome</th>
                                        <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Office: activate to sort column ascending" style="width: 71px;">Valor</th>
                                        <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Age: activate to sort column ascending" style="width: 31px;">Forma</th>
                                        <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 69px;">Data</th>
                                        <th class="sorting" tabindex="0" aria-controls="datatable" rowspan="1" colspan="1" aria-label="Start date: activate to sort column ascending" style="width: 69px;">Parcela</th>


                                </thead>

                                <tbody>
                                    @if (Model != null)
                                    {
                                        foreach (var item in Model)
                                        {
                                            <tr class="even">

                                              
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.nmParticipante)
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Valor)
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.dsTipoPagamento)
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.dtDoacao)
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.nrParcelas)
                                                </td>

                                            </tr>
                                        }
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </section>
</div>

<div class="modal fade" id="modal">

</div>